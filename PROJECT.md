# SRC Hunter 项目说明

> 一个面向安全响应中心（SRC）漏洞赏金场景的**合规资产侦察工具链**，以 MCP Server 形式集成到 Claude Code。

---

## 一、项目定位

**SRC Hunter 解决的核心问题**：漏洞赏金猎人在正式挖洞前，需要投入大量重复劳动做前期侦察——枚举子域名、确认官方测试范围、识别技术栈、梳理攻击面。这部分工作占据了约 80% 的时间，但高度可自动化。

**本项目做的事**：把这 80% 的前期侦察自动化、流程化、合规化，让研究者把精力聚焦在真正需要人工的漏洞分析上。

**本项目不做的事**：不自动挖洞、不自动提交漏洞、不替代人工验证。主动测试与漏洞确认仍需人工 + 专业工具（如 Burp Suite）。

---

## 二、背景与动机

项目的直接触发点是评估「在 Edge 浏览器中对 SRC 平台做自动化漏洞挖掘」的可行性。经过分析，确认了以下现实：

1. **完全自动化挖洞不可行**：逻辑漏洞、越权、复杂漏洞链需要深度业务理解，AI 无法替代。
2. **前期侦察高度可行**：子域名枚举、指纹识别、端点提取等技术成熟，可完全自动化。
3. **合规是硬约束**：SRC 平台有明确的测试范围和规则，超范围扫描违法，AI 报告需人工验证。

因此，项目收敛为「合规侦察工具链」这一务实定位。

---

## 三、架构

```
┌─────────────────────────────────────────────┐
│           Claude Code (对话层)               │
│         /src-recon skill 工作流              │
└──────────────────┬──────────────────────────┘
                   │ MCP 协议 (JSON-RPC over stdio)
┌──────────────────▼──────────────────────────┐
│          SRC Hunter MCP Server               │
│  ┌────────────┬────────────┬──────────────┐  │
│  │ 安全模块    │ 工具模块    │ 基础设施      │  │
│  ├────────────┼────────────┼──────────────┤  │
│  │ 白名单授权  │ 资产爬取    │ HTTP 客户端   │  │
│  │ 频率限制    │ 子域名枚举  │ 日志(winston) │  │
│  │ 审计日志    │ 指纹识别    │ 类型(Zod)     │  │
│  │ payload过滤 │ 端点提取    │              │  │
│  │            │ 漏洞扫描    │              │  │
│  └────────────┴────────────┴──────────────┘  │
└──────────────────┬──────────────────────────┘
                   │
      ┌────────────┼────────────────┐
      ▼            ▼                ▼
  crt.sh API    HTTP 请求       本地文件系统
  (证书记录)    (目标站点)      (whitelist/assets/audit)
```

**技术栈**：TypeScript + Node.js + MCP SDK + Zod + winston + axios

---

## 四、功能清单

### 轻量版（零外部依赖，已实现并实测）

| 工具 | 功能 | 数据源 | 状态 |
|------|------|--------|------|
| `enumerate-subdomains-lite` | 子域名枚举 | crt.sh 证书记录 | ✅ 实测通过 |
| `web-fingerprint-lite` | Web 指纹识别 | 目标站点 HTTP | ✅ 可过 WAF 403 |
| `extract-endpoints-lite` | API 端点提取 | JS 文件分析 | ✅ 已实现 |
| `port-scan-lite` | 端口探测 | TCP 连接 | ✅ 已实现 |
| `vuln-scan-lite` | 漏洞初扫 | 内置 payload | ✅ 实测通过 |
| `check-authorization` | 授权检查 | 白名单 | ✅ 实测通过 |
| `sync-src-assets` | SRC 资产同步 | 平台官网 | ⚠️ 需适配 |

### 完整版（需外部工具，接口保留未启用）

| 工具 | 依赖 | 状态 |
|------|------|------|
| `enumerate-subdomains` | subfinder/amass | ⏸️ 未装 |
| `port-scan` | nmap | ⏸️ 未装 |
| `vuln-scan` | nuclei（5000+ 模板） | ⏸️ 未装 |
| `directory-scan` | dirsearch | ⏸️ 未装 |

> 未启用原因：用户网络环境连 GitHub 超时，Go 工具链无法安装。轻量版已覆盖 80% 场景。

---

## 五、安全与合规机制

### 1. 授权白名单
- 所有主动操作前强制 `check-authorization`
- 支持精确域名 + CIDR 匹配
- **经验教训**：不用宽泛通配符（如 `*.example.com`），否则非核心子域被误判授权

### 2. 频率限制
- 端口扫描 1 次/10s、漏洞扫描 1 次/60s
- 自适应退避（遇 429/503 自动降速）

### 3. 审计日志
- 所有操作记录到 `data/audit.log`（JSON Lines）
- 含时间戳、工具、目标、结果、耗时

### 4. Payload 安全过滤
- 过滤 `rm -rf`、`DROP TABLE`、破坏性 SQL、命令注入等
- 安全模式默认开启

---

## 六、标准使用流程（6 阶段）

这是封装在 `src-recon` skill 中的标准工作流：

```
阶段 0：确认目标与平台（哪个 SRC、哪个企业）
阶段 1：被动子域名枚举（crt.sh，合法，无需授权）
阶段 2：确认官方测试范围（关键合规步骤，查最新公告）
阶段 3：更新授权白名单（核心域名 vs 枚举结果的分离）
阶段 4：被动指纹识别（技术栈矩阵）
阶段 5：API 端点提取（可选，针对 SPA）
阶段 6：交付侦察报告 + 停止
```

---

## 七、360SRC 实战结论（2026-09-24）

本次实战最重要的产出是**合规认知的修正**：

| 初始假设 | 实际情况 |
|---------|---------|
| 枚举出 740 个子域名都可测 | ❌ 官方范围已收窄至 **19 个核心域名** |
| 工具可自动挖洞提交 | ❌ AI 报告必须人工验证 |
| 广泛信息收集高价值 | ⚠️ 价值随范围收窄而降低 |

**关键规则**（360SRC 2026-09-24 公告，有效期至 2026-10-31）：
- 仅收录核心业务/核心资产漏洞，及能影响企业内网的高风险漏洞
- 灵枢计划、X·Safe 计划暂时关闭
- AI/自动化报告须先人工验证
- RCE/深度利用须提前申请，周末节假日不开放

---

## 八、已知限制

1. **网络环境**：GitHub 下载超时、境外靶场不可达、360.cn 需浏览器 UA
2. **检测能力**：轻量版只有 4 类基础检测，远不及 Nuclei 5000+ 模板
3. **SRC 爬虫**：`sync-src-assets` 的选择器是占位示例，需逐平台适配
4. **crt.sh 稳定性**：大企业查询可能 502/超时，已加重试机制

---

## 九、项目结构

```
src-hunter/
├── src/
│   ├── index.ts              # MCP Server 入口（18 个工具注册）
│   ├── types.ts              # Zod 类型定义
│   ├── security/             # 安全模块
│   │   ├── whitelist.ts      #   授权检查
│   │   ├── rate-limit.ts     #   频率限制
│   │   └── audit-log.ts      #   审计 + payload 过滤
│   ├── tools/                # 工具实现
│   │   ├── asset-manager.ts  #   SRC 资产爬取
│   │   ├── recon.ts          #   侦察（外部工具版）
│   │   ├── scanner.ts        #   扫描（外部工具版）
│   │   ├── lite-recon.ts     #   侦察（轻量版）✅
│   │   └── lite-scanner.ts   #   扫描（轻量版）✅
│   └── utils/                # 基础设施
│       ├── http-client.ts    #   浏览器 UA 客户端
│       └── logger.ts         #   winston 日志
├── data/                     # 数据
│   ├── whitelist.json        #   授权白名单
│   ├── assets.json           #   资产库（核心域名 + 枚举参考）
│   └── 360-subdomains.txt    #   360 枚举结果
├── config/                   # 配置
├── docs/                     # 文档
├── scripts/                  # 工具安装脚本
├── dist/                     # 编译产物
├── README.md                 # 技术文档
├── PROJECT.md                # 本文档
└── QUICKSTART.md             # 快速上手
```

---

## 十、可复用资产

除工具链本身，本次还沉淀了两个可复用资产：

1. **`src-recon` skill**（`~/.claude/skills/src-recon/SKILL.md`）
   —— 合规侦察工作流，对新 SRC 平台直接套用

2. **记忆条目**（`src-hunter-toolchain.md`）
   —— 工具链位置、网络限制、技术坑、白名单经验

---

## 十一、合规声明

本工具仅用于**已获授权的安全测试**：

- ✅ 被动侦察（DNS 证书、公开响应头）——合法
- ✅ 官方 SRC 平台范围内的主动测试——合法（需遵守平台规则）
- ❌ 未授权扫描公网资产——违法
- ❌ 超范围测试、破坏性 payload——违法

使用者须自行确认目标授权状态，遵守目标平台规则及当地法律法规。

---

## 十二、总结

**SRC Hunter 是一个完成度较高的合规侦察工具链**，它验证了「自动化辅助 SRC 挖洞」的可行边界：

- 前期侦察（80% 的重复工作）→ 可自动化、可合规化
- 漏洞发现与验证（20% 的核心价值）→ 仍需人工 + 专业工具

项目的真实价值不在于「自动挖洞」，而在于**把研究者从重复的侦察劳动中解放出来**，同时用**强制授权检查**和**官方范围确认**守住合规底线。

---

*项目版本 1.0.0 · 2026-09-24*
